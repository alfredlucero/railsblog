<h1>Hello, Rails! Articles#index</h1>

<p>Our blog has <%= Article.public_count %> public articles and counting!</p>

<ul>
  <%# Controller instance variables can be accessed by the view so we can reference @articles %>
  <%# ERB is a templating system that evaluates Ruby code embedded in a document %>
  <%# %= means evaluate the Ruby code and output value it returns vs. % means evaluate Ruby code %>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li>
        <%# Old way with routeprefix_path i.e. article_path(article) %>
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
